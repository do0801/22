services:
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: taskboard
    ports:
      - "5432:5432"
    volumes:
      - taskboard_db:/var/lib/postgresql/data

volumes:
  taskboard_db:
